---
// Plain paragraphs, e.g. the bio/intro section on both faces. Shell,
// heading, and empty-guard live in Section.astro.
interface Props {
  paragraphs: string[];
}
const { paragraphs } = Astro.props;
---

{paragraphs.map((p) => <p>{p}</p>)}
